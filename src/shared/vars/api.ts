// export const isProduction = () => {
//   if (window.location.host.includes('production') || import.meta.env?.VITE_ODR === 'true') {
//     return true;
//   }
//   return false;
// };

// eslint-disable-next-line max-len
export const API_URL = '';

export enum swrKeys {
  example = 'example',
}